openapi: 3.0.0
info:
  title: API de Prueba
  version: 1.0.0
paths:
  /saludo:
    get:
      summary: Retorna un mensaje de saludo
      responses:
        '200':
          description: Respuesta exitosa
          content:
            application/json:
              schema:
                type: object
                properties:
                  mensaje:
                    type: string
                    example: "¡Hola, mundo!"
